import {
  borderProps,
  colorProps,
  cursorProps,
  displayProps,
  elevationProps,
  flexProps,
  fontProps,
  sizingProps,
  unitsAroundProps,
} from '@shallot-ui/core'

import {
  fontSizeProps,
  letterSpacingProps,
  lineHeightProps,
  radiusProps,
  transitionProps,
} from '../../../props'

export const anchorLinkConfig = {
  ...borderProps,
  ...colorProps,
  ...cursorProps,
  ...displayProps,
  ...elevationProps,
  ...flexProps,
  ...fontProps,
  ...fontSizeProps,
  ...letterSpacingProps,
  ...lineHeightProps,
  ...radiusProps,
  ...sizingProps,
  ...transitionProps,
  ...unitsAroundProps,
}
