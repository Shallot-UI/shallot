import { Meta } from '@storybook/addon-docs'
import { Row, Box, makeTheme } from '@shallot-ui/web'
import { ThemeProvider } from 'styled-components'

<Meta title="Theme / Colors" />

# Color Palettes

In Shallot, color palettes are stored in the colors object within the theme. These palettes are used to provide a consistent and visually pleasing set of colors for your application.

## The Shading Palette

Each color palette is represented by a color name and a set of shade values ranging from 100 to 500 in increments of `25`. The color names in the default theme are `'Primary'`, `'Success'`, `'Warning'`, `'Danger'`, and `'Shading'`.

Below is Sallot's default shading palette. It will be used if no other shading palette is provided in the theme.

<Canvas>
  <ThemeProvider theme={makeTheme()}>
    <Row>
      {[
        'Shading.100',
        'Shading.125',
        'Shading.150',
        'Shading.175',
        'Shading.200',
        'Shading.225',
        'Shading.250',
        'Shading.275',
        'Shading.300',
        'Shading.325',
        'Shading.350',
        'Shading.375',
        'Shading.400',
        'Shading.425',
        'Shading.450',
        'Shading.475',
        'Shading.500',
      ].map((color) => (
        <Box
          key={color}
          backgroundColor={color}
          grow
          shrink
          flex
          unitHeight={5}
          fullWidth
        />
      ))}
    </Row>
  </ThemeProvider>
</Canvas>

## Contextual Color Palettes

The contextual color palettes are used to communicate different states and actions. Each has its own meaning and should be used consistently throughout your application. It's important to note that these colors will be used by some Shallot components by default, so they should not be overridden in ways that don't align with their intended meaning.

### Primary

The primary color palette is used to communicate that an element is the primary action or the primary element on the page. For example, it is used to style the primary button, primary text, and other elements that require a consistent color.

<Canvas>
  <ThemeProvider theme={makeTheme()}>
    <Row>
      {[
        'Primary.100',
        'Primary.125',
        'Primary.150',
        'Primary.175',
        'Primary.200',
        'Primary.225',
        'Primary.250',
        'Primary.275',
        'Primary.300',
        'Primary.325',
        'Primary.350',
        'Primary.375',
        'Primary.400',
        'Primary.425',
        'Primary.450',
        'Primary.475',
        'Primary.500',
      ].map((color) => (
        <Box
          key={color}
          backgroundColor={color}
          grow
          shrink
          flex
          unitHeight={5}
          fullWidth
        />
      ))}
    </Row>
  </ThemeProvider>
</Canvas>

### Success

The success color palette is used to communicate that an element is a success or a positive action. For example, it could be used to style the result of a successful action, or a button that performs a positive action.

<Canvas>
  <ThemeProvider theme={makeTheme()}>
    <Row>
      {[
        'Success.100',
        'Success.125',
        'Success.150',
        'Success.175',
        'Success.200',
        'Success.225',
        'Success.250',
        'Success.275',
        'Success.300',
        'Success.325',
        'Success.350',
        'Success.375',
        'Success.400',
        'Success.425',
        'Success.450',
        'Success.475',
        'Success.500',
      ].map((color) => (
        <Box
          key={color}
          backgroundColor={color}
          grow
          shrink
          flex
          unitHeight={5}
          fullWidth
        />
      ))}
    </Row>
  </ThemeProvider>
</Canvas>

### Warning

The warning color palette is used to communicate that an element is a warning or potentially hazardous action. For example, it could be used to style the result of a file upload where the upload was successful but the file format was incorrect.

<Canvas>
  <ThemeProvider theme={makeTheme()}>
    <Row>
      {[
        'Warning.100',
        'Warning.125',
        'Warning.150',
        'Warning.175',
        'Warning.200',
        'Warning.225',
        'Warning.250',
        'Warning.275',
        'Warning.300',
        'Warning.325',
        'Warning.350',
        'Warning.375',
        'Warning.400',
        'Warning.425',
        'Warning.450',
        'Warning.475',
        'Warning.500',
      ].map((color) => (
        <Box
          key={color}
          backgroundColor={color}
          grow
          shrink
          flex
          unitHeight={5}
          fullWidth
        />
      ))}
    </Row>
  </ThemeProvider>
</Canvas>

### Danger

The danger color palette is used to communicate that an element is a danger or a negative action. For example, it could be used to style the result of a failed action, or a button that performs a negative action. For example a button that deletes an item.

<Canvas>
  <ThemeProvider theme={makeTheme()}>
    <Row>
      {[
        'Danger.100',
        'Danger.125',
        'Danger.150',
        'Danger.175',
        'Danger.200',
        'Danger.225',
        'Danger.250',
        'Danger.275',
        'Danger.300',
        'Danger.325',
        'Danger.350',
        'Danger.375',
        'Danger.400',
        'Danger.425',
        'Danger.450',
        'Danger.475',
        'Danger.500',
      ].map((color) => (
        <Box
          key={color}
          backgroundColor={color}
          grow
          shrink
          flex
          unitHeight={5}
          fullWidth
        />
      ))}
    </Row>
  </ThemeProvider>
</Canvas>

## Converting Palettes

Shading values should adjust their brightness, saturation, and/or hue to create a consistent visual progression. Various tools and libraries exist to help with this process. The default color palettes were generated by adapting similar palettes from [Tailwind CSS](https://tailwindui.com/) using a library called `chroma-js`.

```ts
const convertPalette = (original: string[]) => {
  const shadeValues = [
    100, 125, 150, 175, 200, 225, 250, 275, 300, 325, 350, 375, 400, 425, 450,
    475, 500,
  ] as const

  const shades = chroma.scale(original).colors(shadeValues.length)

  const palette: Record<string, string> = {}
  shadeValues.forEach((shade, index) => {
    palette[shade] = shades[index]
  })

  return palette
}
```
